New-ADOrganizationalUnit -Name LCSEC -Path "DC=LCDC1072,DC=local"

$server_list = Import-Csv -Path C:\Users\Administrator\Downloads\book2.csv

foreach ($server in $server_list) {
    echo $server.name
    echo $server.path
    New-ADOrganizationalUnit -Name $server.name -Path $server.path
}
